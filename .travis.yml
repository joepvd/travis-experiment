language: java
jdk: oraclejdk8
matrix:
    include:
        - env: install=none
        - env: install=oracle
          addons:
            apt:
              packages:
              - oracle-java8-installer
sudo: required
install: skip
script: java -version
notifications:
  email: never
