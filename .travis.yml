language: ruby
sudo: required
matrix:
    include:
        - dist: xenial
          env: dist=xenial
        - dist: trusty
          env: dist=trusty
script: awk '!/^($|#)/' /etc/apt/sources.list /etc/apt/sources.list.d/*
install: true
notifications:
    email: never

