sudo: required
matrix:
    include:
        #- env: image=precise
        #  dist: precise
        - env: image=trusty
          dist: trusty
script: sudo apt-get install -y --force-yes openjdk-7-jdk:i386
install: true
language: generic
notifications:
  email: never
