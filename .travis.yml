language: minimal
sudo: true
group: edge
dist: xenial
install: skip
script:
    - cat /etc/apt/sources.list
    - sudo apt-get update
    - sudo apt-get clean
    - sudo apt-get update
    - sudo apt-get clean
    - sudo sed -i /deb-src/d /etc/apt/sources.list
    - sudo apt-get update
notifications:
  email: never
