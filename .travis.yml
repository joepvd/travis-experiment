language: python
python: 3.7
sudo: true
install: skip
script:
- python --version
- python --version | grep -F 3.7
jobs:
  include:
    - env: dist=xenial
      dist: xenial
    - env: dist=trusty
      dist: trusty
    - env: dist=precise
      dist: precise
notifications:
  email: never
