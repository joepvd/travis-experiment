language: perl
dist: trusty
sudo: false
addons:
  browserstack:
    username: "username"
    access_key: "key"
    verbose: true

perl:
    - 5.24-shrplib
install: perl -V
script: perl -V | grep --color Duseshrplib
notifications:
  email: never
