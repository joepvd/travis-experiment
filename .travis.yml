language: ruby
matrix:
    include:
        - env: test=sudo-deprecated
          dist: trusty
          sudo: required
          group: deprecated-2017Q2
        - env: test=sudo-deprecated
          dist: trusty
          sudo: required
          group: deprecated-2017Q2
filter_secrets: false
install: true
script:
    - sudo apt-get -y install clang lldb-3.8 libicu-dev libtool libcurl4-openssl-dev libbsd-dev build-essential libssl-dev uuid-dev
    - apt list --installed | awk '{print "apt-list:"$0}'
notifications:
    email: never
