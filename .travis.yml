sudo: false
dist: trusty
language: php
install: echo $stage
script: "php <<<'<?php var_dump(gd_info());?>'"
php:
    - 5.6
    - 7.0
jobs:
    include:
        - stage: nil
          env: stage=nil
        - stage: test
          env: stage=test

notifications:
  email: never
