language: python
python: 3.6
sudo: false
dist: trusty
addons:
    postgresql: 9.6
services:
    redis-server
install:
    - sleep 10
script:
    - ls -lR /var/ramfs || true
    - ls -lR /var/lib/postgr* || true
    - cat /etc/passwd
    - cat /etc/group
notifications:
    email: false
