language: php
sudo: false
install:
- ls -la ~
- find ~/virtualenv -name active
- source "$(find ~/virtualenv -name active | head -n 1)"
script:
    - which python
    - python --version
notifications:
  email: never
