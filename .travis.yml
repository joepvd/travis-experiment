language: generic
os: osx
install:
  - "declare -f || true"
  - "declare -F || true"
script:
  - unset -f pushd
  - unset -f cd
  - unset -f popd
  - pushd ~
  - popd
  - cd ..
notifications:
    email: never
