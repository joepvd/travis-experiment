sudo: false
dist: trusty
language: generic
install: true
script: true
before_deploy:
    - pushd $TRAVIS_BUILD_DIR
    - git pull
    - grep $TRAVIS_COMMIT .git/refs/heads/$TRAVIS_BRANCH
    - false
    - popd
deploy:
    provider: script
    script: echo deployed!
notifications:
    email: false
