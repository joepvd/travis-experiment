language: minimal
sudo: true
group: edge
dist: xenial
install: skip
script:
    - systemctl list-units
    - systemctl status
    - pstree -Up
    - head -n 1000 $(find /etc/apt/source* -type f)
notifications:
  email: never
