language: ruby
install: true
script:
    - ps aux | grep memc
    - "telnet localhost 11211 <<<stats &"
    - sleep 100
    - "telnet localhost 11211 <<<stats &"
services:
    - memcached
matrix:
    include:
        - env: image=ec2
          sudo: false
        - env: image=gce
          sudo: required
notifications:
    email: false
