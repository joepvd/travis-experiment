matrix:
  include:
    - sudo: deprecated
    - sudo: required

addons:
  apt_packages:
  - freetds-dev
  - libmcrypt-dev

install: skip
script: gem install tiny_tds -v 1.0.5
after_failure: cat  /home/travis/.rvm/gems/*/extensions/x86_64-linux/*/tiny_tds*/mkmf.log
language: ruby
rvm: 2.3.4
notifications:
  email: true
