sudo: false
dist: trusty
language: php
php: 5.6
install: echo $stage
script: "php <<<'<?php var_dump(gd_info());?>'"
jobs:
    include:
        - stage: nil
          env: stage=nil
        - stage: test1
          env: stage=test

notifications:
  email: never
