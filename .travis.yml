language: sh
sudo: false
stages:
    - Build
    - Test
    - Deploy
env:
    - test=1
    - test=2
install: true

jobs:
    include:
        - stage: build
          script: echo stage build
        - stage: test
          script: echo $test
        - stage: deploy
          script: echo deploy



notifications:
    email: never

