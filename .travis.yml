name: arch=ppc64le
arch: ppc64le
dist: xenial
language: python
python: "3.7.0"
install:
  - which python
  - python --version
  - python -c 'import time;print(time.time())'
script: skip
notifications:
  email: false
