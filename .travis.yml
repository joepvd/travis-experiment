language: minimal
sudo: true
#group: edge
dist: trusty
install: skip
script:
    - cat /etc/apt/sources.list
    - sudo apt-get update
    - sudo apt-get clean
    - sudo rm /var/lib/apt/lists/*
    - sudo apt-get update
    - sudo apt-get clean
    - sudo rm /var/lib/apt/lists/*
    - sudo sed -i /deb-src/d /etc/apt/sources.list
    - sudo apt-get update
notifications:
  email: never
