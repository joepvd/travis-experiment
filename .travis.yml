language: python
dist: trusty
sudo: required
install: skip
services: mongod
jobs:
    include:
        - env: dist=precise
          dist: precise
        - env: dist=trusty
          dist: trusty
        - env: dist=xenial
          dist: xenial
script: mongo --eval 'db.version()'
notifications:
  email: never
