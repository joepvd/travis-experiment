sudo: required
language: ruby
dist: trusty
group: stable
before_install:
    - ps -elf
    - pstree -ap
    - sudo lsof | grep /dev/pts
    - shopt
    - echo $-
install:
    - echo sleep 900 >>trap.sh
script:
    - setsid sleep 3 &
    - echo $hiddenvar1
after_script:
    - ps -elf
    - pstree -ap
    - sudo lsof | grep /dev/pts
    - shopt
    - echo $-
    - ps xao pid,ppid,pgid,sid,comm
notifications:
    email: false
