sudo: false
matrix:
    include:
        - env: dist=trusty job=1
          dist: trusty
        - env: dist=precise job=1
          dist: precise
addons:
    apt:
        packages:
            - sysbench
script:
    - sysbench --test=cpu run
language: generic
notifications:
  email: never
