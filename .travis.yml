
language: bash
sudo: false
dist: trusty
install:
    - eval `ssh-agent -s`
    - sleep 1
script:
    - ls -l /proc/$(pgrep ssh-agent)/fd
    - ls -l /proc/$$/fd
    - ps -jef --forest
notifications:
    email: never
