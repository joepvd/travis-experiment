language: node_js
sudo: required
matrix:
    include:
        - env: image=precise run=1
          dist: precise
script: curl -sL http://ppa.launchpad.net/chris-lea/redis-server/ubuntu/dists/precise/InRelease
install: while (( i++ < 10 )); do traceroute ppa.launchpad.net; sleep 1; done
notifications:
    email: never
