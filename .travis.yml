_helpers:
    - &include1
      script: echo included
      install: *include2
    - &include2 echo install from include2
jobs:
    include:
        - <<: *include1
        - stage: Niceness
          script: echo second stage
sudo: false
dist: trusty
script: true
install": true
notifications:
  email: never
