language: ruby
jobs:
  include:
    - env: dist=trusty
      dist: trusty
    - env: dist=xenial
      dist: xenial
    - env: dist=precise
      dist: precise
services:
  - postgresql
install: skip
script:
  - find / -name '*libssh*' 2>/dev/null
  - ldd $(which sshd)
notifications:
  email: false
