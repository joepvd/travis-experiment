language: minimal # This ensures no MySQL is installed
sudo: required
dist: trusty
apt:
  packages:
  - mysql-server-5.5
  - mysql-client-5.5
install:
    - apt-cache search mysql-server
script:
    - mysql -uroot -e "show variables like '%version%';"
notifications:
    email: never
