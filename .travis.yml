sudo: false
matrix:
    include:
        - env: dist=precise
          dist: precise
        - env: dist=trusty
          dist: trusty
install: true
script:
    - gem --version
    - gem update --system --no-document --no-post-install-message
    - gem --version
language: ruby
notifications:
  email: never
