language: scala
sudo: false
install: true
script:
    - which python
    - for p in $(tr : \   <<<$PATH); do ls -l $p/python* || true; done
    - which pip
    - for p in $(tr : \   <<<$PATH); do ls -l $p/pip* || true; done
notifications:
    email: never
