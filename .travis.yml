
language: php
php: 5.6
sudo: required
dist: trusty
install:
    - sudo apt-get install -y mysql-server-5.7 mysql-client-5.7
script:
    - mysql -h localhost "show variables like '%version%'"
notifications:
    email: never
