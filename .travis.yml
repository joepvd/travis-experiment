language: bash
sudo: false
matrix:
    include:
        - dist: trusty
          env: dist=trusty
        - dist: precise
          env: dist=precise
install: true
script: df -h /dev/shm
notifications:
    email: false
