language: bash
install: skip
os: osx
osx_image: xcode10
script:
    - make hello
    - 'rm -f `which gtar`'
    - 'which gtar && false || true'
    - 'make hello && false || true'
notifications:
  email: false
