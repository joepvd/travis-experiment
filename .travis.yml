language: bash
os: osx
osx_image: xcode8.3
install: true
script:
    - echo $should_not_show_up
    - echo $a_space_b_backslashed   
    - echo $c_space_d_quoted   
    - echo $e_space_f_without_quote
    - grep -r shell_session_update ~
notifications:
    email: false
