language: generic
dist: trusty
sudo: false
matrix:
    include:
        - env: filter=yes
        - env: filter=no
          filter_secrets: false
script:
    - echo $should_not_show_up
    - echo $a_space_b_backslashed   
    - echo $c_space_d_quoted   
    - echo $e_space_f_without_quote
install: true
notifications:
    email: false
