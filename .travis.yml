language: ruby
sudo: required
dist: precise
script:
    - sudo apt-get update
    - "[ -f /etc/apt/sources.list.d/mongodb-3.4.list ] && grep -vq arch=amd64 /etc/apt/sources.list.d/mongodb-3.4.list && sudo sed 's/^deb /deb [arch=amd64] /' /etc/apt/sources.list.d/mongodb-3.4.list"
    - sudo apt-key adv --keyserver hkp://keyserver.ubuntu.com:80 --recv 0C49F3730359A14518585931BC711F9BA15703C6
install: echo install
notifications:
    email: never
