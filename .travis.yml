language: generic
dist: doesnotexist
matrix:
    include:
        - env: image=ec2
          sudo: false
        - env: image=gce
          sudo: required
install: true
script: true
notifications:
    email: never
