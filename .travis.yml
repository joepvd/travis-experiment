language: minimal
sudo: false
dist: trusty
filter_secrets: false
install:
    - exec 1> >(awk '{print "filter!", $0}') 2>&1
script:
    - true
after_script:
    - exec >/dev/tty 2>&1
    - sync
notifications:
    email: never
