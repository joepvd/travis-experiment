language: bash

matrix:
  include:
    - env: infra=ec2
      sudo: false
    - env: infra=gce
      sudo: required
      dist: trusty
    - env: infra=mac
      os: osx
      language: swift
      osx_image: xcode8.3

install: true

script: awk 'BEGIN{while(1)print "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaa"}'

notifications:
    email: never
