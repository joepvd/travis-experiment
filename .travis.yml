language: sh
sudo: false
stages:
    - Build
    - Test
    - Deploy
install: true

jobs:
    include:
        - stage: build
          script: echo stage build
        - stage: test
          script: echo $test
          env:
              - test=1
              - test=2
        - stage: deploy
          script: echo deploy



notifications:
    email: never

