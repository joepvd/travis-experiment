language: generic

sudo: false

env:
    matrix:
        - fail=yes
        - fail=no

install:
  - true

script:
  - "[[ $fail = yes ]] && false || true"

after_script:
    - echo Afterscript runs with fail $fail

after_failure:
    - echo Afterscript runs with fail $fail

notifications:
  email: false

