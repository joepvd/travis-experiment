language: bash
sudo: false
install: skip
script:
    - "curl -6sSL ifconfig.co || true"
    - curl -4 ifconfig.co
    - grep -F "$(curl -4sSL ifconfig.co)" <(dig +short nat.travisci.net)
    - echo $TRAVIS_APP_HOST
notifications:
  email: false
