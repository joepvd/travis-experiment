language: sh
sudo: false
stages:
    - Build
    - Test
    - Deploy
env:
    - test=1
    - test=2
script: echo $test
install: true
jobs:
    include:
        - stage: build
          script: echo stage build
          env:
            - test=
        - stage: deploy
          script: echo deploy
          env:
            - test=

notifications:
    email: never

