dist: trusty
sudo: required
language: ruby
install: true
script:
    - sudo service mysql start; echo $?
    - sudo service mysql start; echo $?
    - sudo service mysql start; echo $?
    - sudo service doesnotexist start; echo $?
notifications:
    email: false
