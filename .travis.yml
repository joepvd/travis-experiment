sudo: false
matrix:
    include:
        - env: dist=trusty
          dist: trusty
        - env: dist=precise
          dist: precise
env:
    - job=1
    - job=2
    - job=3
    - job=4
    - job=5
script: time gawk -f prime.awk 8000 proc
language: generic
notifications:
  email: never
