language: bash
sudo: false
stages:
    - part 1
    - part 2
jobs:
    include:
    - stage: part 1
      script: echo yes
    - stage: part 2
      script: echo stage 2
      if: (NOT type IN (pull_request)) AND (branch = master)
notifications:
  email: never
