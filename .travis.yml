sudo: required
dist: trusty
language: generic
matrix:
    include:
        - env: fix=true
          install: sudo apt-get update
        - env: fix=false
          install: true
script: sudo apt-get install -y --force-yes openjdk-7-jdk:i386
notifications:
  email: never
