language: node_js
node_js: 6

matrix:
    include:
        - env: stack=trusty-container
          sudo: false
          dist: trusty
        - env: stack=trusty-sudo
          sudo: required
          dist: trusty
        - env: stack=trusty-container
          sudo: false
          dist: trusty
        - env: stack=trusty-sudo
          sudo: required
          dist: trusty

addons:
  apt:
    sources:
    - google-chrome
    packages:
    - google-chrome-stable
