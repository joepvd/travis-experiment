language: generic

sudo: false

env:
    matrix:
        - pass=yes
        - pass=no

install:
  - "[[ $pass = yes ]] && true || false"

before_script:
  - true

script:
    - echo "script runs"

after_script:
    - echo Afterscript runs with fail $fail

after_failure:
    - echo Afterscript runs with fail $fail

notifications:
  email: false

