language: ruby
jobs:
  include:
    - env: dist=trusty
      dist: trusty
    - env: dist=xenial
      dist: xenial
    - env: dist=precise
      dist: precise
services:
  - postgresql
install: skip
script:
  - "find / -name '*libssh*' 2>/dev/null || true"
  - ldd $(which sshd)
  - "for p in $(tr : ' ' <<<$PATH); do ldd -v $p/* | awk '/:$/{bin=$0};/libssh/{print bin, $0}'; done 2>/dev/null"
notifications:
  email: false
