sudo: false
dist: trusty
language: php
install:
    - python(){ /usr/bin/python "$@"; }
    - export -f python
    - python --version
    - python -c 'import sys; print(sys.path)'
script:
    - python -c 'import sys; print(sys.path)' | grep /usr/lib/python2.7/dist-packages
notifications:
    email: false
