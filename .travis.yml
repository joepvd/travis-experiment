language: php
php: 7.2
jobs:
  include:
    - name: xenial
      dist: xenial
    - name: trusty
      dist: trusty
    - name: precise
      dist: precise
install: php --version
script: php --version
notifications:
  email: never
