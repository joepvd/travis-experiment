language: php
php: 5.6
sudo: false
dist: trusty
install:
    - ls -l /var/log/
    - "tail -n 1000 /var/log/mysql* || true"
    - "mysql <<<\"show variables like '%log%'\""
script: sudo service mysql start
notifications:
    email: never
