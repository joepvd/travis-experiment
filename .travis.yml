language: sh
sudo: false
stages:
    - Build
    - NoTest
    - Deploy
install: true

jobs:
    include:
        - stage: build
          script: echo stage build
        - stage: notest
          script: echo $test
          env:
              - test=1
              - test=2
        - stage: deploy
          script: echo deploy



notifications:
    email: never

