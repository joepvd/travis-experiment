language: ruby
sudo: required
dist: trusty
install:
    - echo -e '[mysqld]\nperformance_schema=OFF\n' | sudo tee -a /etc/mysql/my.cnf
    - sudo service mysql restart
    - sleep 3
script: mysql -uroot <<<'show variables like "%schema%";'
notifications:
  email: never
