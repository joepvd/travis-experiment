language: generic
sudo: required
matrix:
    include:
        - env: group=
          group: deprecated-2017Q2
        - env: group=stable
dist: trusty
install: true
script:
    - sudo grep -Ri hhvm /etc/apt
    - sudo sh -c 'head -n 1000 $(find /etc/apt -type f)'


notifications:
    email: never
