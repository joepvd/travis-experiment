language: ruby
sudo: false
dist: trusty
install:
    - "echo set -e >>file.sh"
    - echo 'if false; then echo will not print; elif false; then echo not printed; else should be printed; fi' >>file.sh
    - trap 'travis_terminate 1' INT QUIT TERM ERR
script:
    - source file.sh
notifications:
    email: never
