language: ruby
sudo: required
matrix:
    include:
        - env: image=trusty
          dist: trusty
        - env: image=precise
          dist: precise
dist: trusty
install: true
script: docker --version
notifications:
    email: never
