sudo: false
dist: trusty
language: generic
install: true
script: true
after_success:
    - pushd $TRAVIS_BUILD_DIR
    - git pull $TRAVIS_BRANCH
    - grep $TRAVIS_COMMIT .git/refs/heads/$TRAVIS_BRANCH
    - false
    - popd
deploy:
    provider: script
    script: echo deployed!
notifications:
    email: false
