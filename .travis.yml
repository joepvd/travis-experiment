language: ruby
sudo: required
addons:
    apt:
        sources:
            - mongodb-3.2-trusty
              #packages:
              #packagmongodb-org-server
install: sleep 3
script:
    - "for p in $(apt-cache search mongo | awk '/^mongo/{print $1}'); do apt-cache madison $p; done"
    - sudo apt-get install -y --allow-downgrades mongodb-org=3.2.18
notifications:
    email: never
