language: c
osx_image: xcode6.4
install: skip
script:
    - which python
    - python version
    - "which pip || true"
    - python -m ensure_pip
    - which pip
    - pip --version
notifications:
  email: never
