language: generic
sudo: false
jobs:
    include:
        - if: type = pull_request
          script: echo push
        - if: type in (pull_request)
          script: echo push
install: true
script: head -n 1000 /etc/hosts*
notifications:
    email: never
