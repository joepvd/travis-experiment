language: generic
env:
    global:
    - CLOUDSDK_CORE_DISABLE_PROMPTS=1
    - GCLOUD_SDK_PATH=$HOME/google-cloud-sdk
matrix:
    include:
        - env: image=ec2
          sudo: false
          script: curl https://dl.google.com/dl/cloudsdk/channels/rapid/install_google_cloud_sdk.bash | bash
          #- env: image=gce
          #- sudo: required
          #- script: curl https://sdk.cloud.google.com | sed 's/zxcf/zxf/' | bash
          #- env: image=gce-deprecated
          #- grouyp: deprecated-2017Q4
          #- sudo: required
          #- script: curl https://sdk.cloud.google.com | sed 's/zxcf/zxf/' | bash
install: true
notifications:
    email: never
