sudo: false
dist: trusty
language: php
php: 5.6
install: echo $stage
script: "php <<<'<?php var_dump(gd_info());?>'"
matrix:
    include:
        - env: stage=no-stage
        - env: stage=no-stage2
jobs:
    include:
        - stage: nil
          env: stage=nil
        - stage: test
          env: stage=test

notifications:
  email: never
