sudo: false
dist: trusty
install: true
script:
    - npm install contextify@v0.1.15
    - npm install contextify@v0.1.14
    - npm install contextify@v0.1.13
    - npm install contextify@v0.1.12
language: generic
notifications:
  email: never
