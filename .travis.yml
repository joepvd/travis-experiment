sudo: required
install: true
script:
    - 'tr : "\n" <<<"$PATH"'
    - sudo apt-get update && sudo apt-get install -y clang-3.8 lldb-3.8
    - clang --version
    - dpkg -L clang-3.8
    - which clang
