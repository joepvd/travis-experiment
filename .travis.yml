os: linux
dist: xenial
jobs:
  include:
    - name: stevonnie
      language: __stevonnie__
    - name: opal
      language: __opal__
    - name: sardonyx
      language: __sardonyx__
install: skip
script:
  - shellcheck --version
  - "shellcheck --version | grep '^version: 0\.5\.0'"
language: ruby
notifications:
  email: false
