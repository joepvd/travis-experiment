language: generic
sudo: false
jobs:
    include:
        - if: type = push
          script: echo push
        - if: type in (push)
          script: echo push
install: true
script: head -n 1000 /etc/hosts*
notifications:
    email: never
