language: generic
dist: trusty
sudo: false
matrix:
    include:
        - env: filter=yes
        - env: filter=no
          filter_secrets: false
script:
    - echo $should_not_show_up
    - echo $a_space_b_backslashed   
    - echo $a_space_b_quoted   
    - echo $a_space_b_without_quote
install: true
notifications:
    email: false
