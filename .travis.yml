sudo: false
dist: trusty
language: node_js
node: 7
install: true
script:
    - yarn --version
    - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 0.17.8
    - yarn --version
notifications:
  email: never
