sudo: false
dist: trusty
install: true
script: npm install contextify
language: generic
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
notifications:
  email: never
