dist: xenial
language: __sardonyx__
jobs:
  include:
    - name: stable
      group: stable
    - name: dev
      group: dev
    - name: edge
      group: edge
script:
  - which firefox
  - firefox --version
language: ruby
notifications:
  email: false
