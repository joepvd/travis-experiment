language: python

sudo: required

dist: precise

services:
    - mongodb

install:
    - "ps  aux | grep '[m]ongo' || true"
    - "sleep 10"
    - "pgrep mongo && sudo lsof -p $(pgrep mongo)"
    - sudo service mongodb status


script:
    - echo script

notifications:
  email: false
