language: minimal
sudo: false
dist: trusty
matrix:
    include:
        - env: filter=yes
        - env: filter=no
          filter_secrets: false
install:
    - true
script:
    - ls -l /proc/$$/fd
notifications:
    email: never
