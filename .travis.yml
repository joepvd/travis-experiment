language: python
sudo: required
dist: trusty
install:
    - sudo service mysql stop
    - until (( i++ > 10 )) || { mysql <<<'select 1;' && echo connected; }; do echo $i; sleep 1; done &
script: sudo service mysql start
notifications:
    email: never
