language: ruby
sudo: false
group: dev
dist: trusty
services:
    - mysql
install:
    - sleep 10
script:
    - mysql -BNe "show global variables"
    - mysql -BN -h 127.0.0.1 -u root -e "show global variables"
notifications:
    email: false
