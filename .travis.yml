language: php
php: "7.0"
matrix:
    include:
        - env: image="sudo trusty stable"
          dist: trusty
          group: stable
          sudo: required
        - env: image="sudo trusty apt mysql"
          dist: trusty
          sudo: required
          addons:
              apt:
                  packages:
                      - mysql-server-5.6
                      - mysql-client-core-5.6
                      - mysql-client-5.6
install: true
script: 
    - "ps aux | grep '[m]ysql' || true"
    - "mysql -uroot -e 'SHOW VARIABLES LIKE \"%version%\";' || true"
    - "mysql -uroot -e 'SHOW VARIABLES LIKE \"%sock%\";' || true"
    - "mysql -uroot -e 'SHOW VARIABLES' || true"
notifications:
    email: false
