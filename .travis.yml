jobs:
    include:
        - stage: stage
          install: true
        - stage: stage
          install: true
        - stage: stage
          install: true
sudo: false
dist: trusty
script: "false"
notifications:
  email: never
