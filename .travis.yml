jobs:
  include:
    - name: precise
      dist: precise
    - name: trusty
      dist: trusty
    - name: xenial
      dist: xenial
language: python
install: skip
script:
  - grep -rE 'rabbit|packagecloud' /etc/apt
notifications:
  email: false
