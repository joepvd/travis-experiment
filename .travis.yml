language: bash
sudo: required
dist: xenial
matrix:
  include:
    - env: group=unset
    - env: group=stable
      group: stable
    - env: group=edge
      group: edge
    - env: group=dev
      group: dev
    - env: group=invalid
      group: invalid
install: skip
script: skip
notifications:
  email: false
