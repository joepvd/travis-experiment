language: python
sudo: false
dist: trusty
addons:
    postgresql: 9.6
install:
    - sleep 10
script:
    - ls -lR /var/ramfs || true
    - ls -lR /var/lib/postgr* || true
    - cat /etc/passwd
    - cat /etc/group
notifications:
    email: false
