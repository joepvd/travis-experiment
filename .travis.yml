language: ruby
sudo: false
dist: trusty
install:
    - "echo set -e >>file.sh"
    - echo 'if false; then echo will not print; elif false; echo not printed; else echo other condition; fi' >>file.sh
    - trap 'travis_terminate 1' INT QUIT TERM
script:
    - source file.sh
notifications:
    email: never
