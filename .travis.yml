language: python
python: 3.6
install: true
script:
    - echo -e 'stats\r' | nc localhost 11211
    - ps aux | grep memc
services:
    - memcached
matrix:
    include:
        - env: image=ec2
          sudo: false
        - env: image=gce
          sudo: required
notifications:
    email: false
