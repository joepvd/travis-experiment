sudo: required
language: python
python:
- '2.7'
arch: ppc64le
install:
    - sudo ls -l /home
    - cat /etc/passwd
env:
    - run=1
    - run=2
    - run=3
    - run=4
    - run=5
    - run=6
    - run=7
    - run=8
    - run=9
    - run=10
script: grep solarce /etc/passwd
notifications:
  email: false
