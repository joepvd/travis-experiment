language: sh
sudo: false
stages:
    - Build
    - Test
    - Deploy
install: true

jobs:
    include:
        - stage: build
          script: echo stage build
        - stage: test
          script: echo $test
        - stage: deploy
          script: echo deploy

env:
  matrix:
  - test=1
  - test=2


notifications:
    email: never

