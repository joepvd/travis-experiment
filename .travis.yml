language: java
jdk: openjdk6
matrix:
    include:
        - env: group=deprecated-sudo
          group: deprecated-2017Q1
          sudo: required
        - env: group=current-sudo
          sudo: required
        - env: group=current-container
          sudo: false
dist: trusty
services:
    - mysql
install: true
script:
    - ls -l /usr/lib/jvm/
    - ls -l /usr/lib/jvm/*1.6*open*
notifications:
    email: never
