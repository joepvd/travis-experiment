language: ruby
sudo: required
dist: xenial
install:
    - ps aux --forest
    - ls -lR /var/lib/dpkg/lock
    - "sudo fuser $(find /var/lib/dpkg/lock -type f) || true"
script: sudo apt-get install rxvt-unicode
after_script: ps aux --forest


notifications:
    email: never

