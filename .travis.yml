language: generic
matrix:
    include:
        - env: stack=trusty-stable-container
          sudo: false
        - env: stack=trusty-sudo-stable
          sudo: required
        - env: stack=osx
          language: swift
          osx_image: xcode8.3
install: true
script: awk 'BEGIN{for (i=1;;i++)print "4 mb log limit"}'
notifications:
    email: false

