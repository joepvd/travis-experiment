language: ruby
sudo: false
dist: trusty
services:
    - redis-server
install:
    - initctl list || true
    - service --status-all
    - cat /etc/init.d/redis-server
script: redis INFO
notifications:
    email: false
