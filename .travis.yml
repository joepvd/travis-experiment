language: ruby
sudo: required
dist: trusty
install:
    - sudo mysql_upgrade
    - sudo service mysql restart
    - sleep 3
script:
    - mysql -uroot -e "show variables like '%version%';"
notifications:
    email: never
