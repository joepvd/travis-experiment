sudo: true
dist: trusty
language: ruby
install:
    - true
services:
    - mysql
apt:
    packages:
        - mysql-server
script:
    - "mysql --protocol=TCP --host=localhost --user root -e \"CREATE USER 'nice'@'localhost' IDENTIFIED BY 'nice'\" || true"
notifications:
    email: false
