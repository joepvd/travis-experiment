language: ruby
matrix:
  include:
  - os: linux
    sudo: false
    dist: trusty
    env: DIST=trusty-stable
install: true
script:
- travis_wait 15 ( echo first; sleep 660; echo last ) >log
notifications:
    email: never
