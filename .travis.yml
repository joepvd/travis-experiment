jobs:
  include:
    - name: precise
      dist: precise
    - name: trusty
      dist: trusty
    - name: xenial
      dist: xenial
language: bash
install: skip
script:
  - cat ~/.rvmrc
  - 'cat /etc/rvmrc || true'
  - gem install travis
  - "[[ -f ~/.rvmrc ]] && sed 's/--no-ri//g' ~/.rvmrc"
  - gem install travis
notifications:
  email: true
