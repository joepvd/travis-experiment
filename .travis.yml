language: php
sudo: false
install:
- ls -la ~
- find ~/virtualenv -name activate
- source "$(find ~/virtualenv -name activate | head -n 1)"
script:
    - which python
    - python --version
notifications:
  email: never
