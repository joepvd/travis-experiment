language: ruby
sudo: false
dist: trusty
env:
    global:
        - ULIMIT=
services:
    - dbus
    - redis-server
install:
    - initctl list || true
    - service --status-all
    - cat /etc/init.d/redis-server
script: redis-cli INFO
notifications:
    email: false
