language: php
php: 5.6
sudo: required
matrix:
    include:
        - env: dist=trusty
          dist: trusty
        - env: dist=precise
          dist: precise
services:
    - mysql
install: true
script:
    - mysql <<<'select * from mysql.user;'
    - mysql <<<'desc mysql.user;'
notifications:
    email: never
