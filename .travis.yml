language: bash
install: skip
script:
  - sudo add-apt-repository -y ppa:chris-lea/redis-server
  - sudo apt-get update
  - sudo apt-cache madison redis-server
  - sudo apt-get install redis-server
  - sudo journalctl -b
dist: xenial
sudo: false
notifications:
  email: false
