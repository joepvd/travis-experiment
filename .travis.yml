jobs:
  include:
    - name: trusty
      dist: trusty
    - name: xenial
      dist: xenial
language: php
php: 7.3snapshot
install: skip
script:
  - which php
  - ~/.phpenv/versions/7.3snapshot/bin/php --version
  - ls -l ~/.phpenv/versions
  - ldd ~/.phpenv/versions/7.3snapshot/bin/php
notifications:
  email: false
