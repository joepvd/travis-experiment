language: generic
dist: precise
sudo: false
addons:
    apt:
        packages:
            - curl
install: curl --version
script: curl -vX POST https://vixlet.deploybot.com/webhook/deploy
notifications:
  email: never
