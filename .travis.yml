language: generic
dist: precise
sudo: false
addons:
    apt:
        packages:
            - chromium-chromedriver
            - chrome-browser
script:
    - while sleep 1; do top -b -n 1 | head -n 20; done &
    - sleep 10
    - chrome --version || chromium --version
install: true
notifications:
    email: false
