language: generic
sudo: required
matrix:
    include:
        - env: image=stable
        - env: image=old
          group: deprecated-2017Q4
install: head /proc/sys/fs/pipe*
script:
    - awk 'BEGIN{for(i=1;i<=3000;i++)a=a"\naaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa";print a}'
notifications:
    email: never
