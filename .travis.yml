language: php
php: 5.6
sudo: required
matrix:
    include:
        - env: dist=trusty
          dist: trusty
services:
    - mysql
install: true
script:
    - mysql <<<'select * from mysql.user;'
    - mysql -uroot <<<"GRANT ALL PRIVILEGES ON *.* TO 'travis';"
    - mysql <<<'select * from mysql.user;'
    - mysql <<<'desc mysql.user;'
notifications:
    email: never
