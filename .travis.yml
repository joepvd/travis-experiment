language: python
python: 2.7
sudo: required

services:
    - docker
install: nslookup -query=AAAA vwp7884.webpack.hosteurope.de
script:
    - sudo find /var/lib/postgres
notifications:
    email: never
