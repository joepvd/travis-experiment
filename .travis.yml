install: skip
script:
  - grep -r travis-ci /etc/apt/sources*
  - head -n 1000 $(grep -rl travis-ci /etc/apt/sources*)
dist: precise
sudo: required
group: stable
language: ruby
notifications:
  email: false
