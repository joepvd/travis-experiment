language: generic
sudo: required
matrix:
    include:
        - env: run=stable
        - env: run=deprecated
          group: deprecated-2017q4
script:
    - docker run -it alpine /bin/sh -c 'ip -6 address show'
    - docker run -it alpine /bin/sh -c 'ip -4 address show'
notifications:
    email: never
