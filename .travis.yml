language: python
sudo: false
dist: trusty
install:
    - sleep 10
script:
    - ls -lR /var/ramfs || true
    - ls -lR /var/lib/postgr* || true
    - cat /etc/passwd
    - cat /etc/group
notifications:
    email: false
